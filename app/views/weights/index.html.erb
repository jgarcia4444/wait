<div class="container text-center">
    <h1><%= @user.name %>'s Weights</h1>
    <div class="floating-container-blue form">
        <div>
            <% @weights.reverse.each do |weight| %>
                <p><%= weight.display_date %></p>
                <p><%= weight.weight_input %></p>
            <% end %>
        </div>
    </div>
</div>